namespace = sb_firearms

# Firearms JE completion flavor. The heavy lifting lives in
# common/journal_entries/1-04_sb_firearms_acquisition.txt and
# common/scripted_effects/sb_firearms_effects.txt; this event is the visible
# payoff shell once a country finishes modernizing.

sb_firearms.001 = {
	type = country_event
	placement = root

	title = sb_firearms.001.t
	desc = sb_firearms.001.d
	flavor = sb_firearms.001.f

	event_image = {
		video = "unspecific_military_parade"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"
	icon = "gfx/interface/icons/event_icons/event_military.dds"
	duration = 3

	trigger = {
	}

	option = {
		name = sb_firearms.001.a
		add_modifier = {
			name = sb_new_tactics
			days = 1825
		}
		ruler = {
			if = {
				limit = {
					NOT = { has_trait = innovative }
				}
				add_trait = innovative
			}
		}
		if = {
			limit = {
				country_definition = cd:ZUL
			}
			sb_zulu_add_dynastic_stability_20 = yes
		}
	}
}
