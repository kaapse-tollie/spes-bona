sb_purge_starting_buildings_effect = {
	if = {
		limit = { any_scope_building = { is_building_type = building_government_administration } }
		remove_building = building_government_administration
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_construction_sector } }
		remove_building = building_construction_sector
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_trade_center } }
		remove_building = building_trade_center
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_port } }
		remove_building = building_port
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_university } }
		remove_building = building_university
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_barrack } }
		remove_building = building_barrack
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_conscription_center } }
		remove_building = building_conscription_center
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_arms_industry } }
		remove_building = building_arms_industry
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_textile_mill } }
		remove_building = building_textile_mill
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_wheat_farm } }
		remove_building = building_wheat_farm
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_maize_farm } }
		remove_building = building_maize_farm
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_millet_farm } }
		remove_building = building_millet_farm
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_livestock_ranch } }
		remove_building = building_livestock_ranch
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_logging_camp } }
		remove_building = building_logging_camp
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_fishing_wharf } }
		remove_building = building_fishing_wharf
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_cotton_plantation } }
		remove_building = building_cotton_plantation
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_tea_plantation } }
		remove_building = building_tea_plantation
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_tobacco_plantation } }
		remove_building = building_tobacco_plantation
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_sugar_plantation } }
		remove_building = building_sugar_plantation
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_banana_plantation } }
		remove_building = building_banana_plantation
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_coffee_plantation } }
		remove_building = building_coffee_plantation
	}
	if = {
		limit = { any_scope_building = { is_building_type = building_vineyard } }
		remove_building = building_vineyard
	}
}
