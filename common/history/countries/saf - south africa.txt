# South Africa (SAF) — Spes Bona Override: SAF is now a formable Union of South Africa
# In vanilla V3, SAF is the active starting British colony at the Cape.
# This mod replaces that with CAP (Cape Colony) as the active tag.
# SAF is reserved as a formable nation — the Union of South Africa (est. 1910 historically).
# See: common/history/countries/cap - cape colony.txt (inherits all vanilla SAF setup)
# All original SAF content (laws, IGs, techs, institutions) was moved to CAP.
# This file overrides vanilla saf - south africa.txt by clearing its setup.
COUNTRIES = {
	c:SAF ?= {
		# SAF exists only as a formable — no starting setup.
		# All original SAF content (laws, IGs, techs) moved to CAP.
		# Formation conditions are defined in the journal entry / decision files.
	}
}
