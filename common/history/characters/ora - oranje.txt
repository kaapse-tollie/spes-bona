CHARACTERS = {
	c:ORA ?= {
		# Use vanilla-linked trekker templates where possible so ORA keeps stable
		# historical appearances across launches.
		create_character = {
			template = ORA_hendrik_potgieter
		}

		create_character = {
			template = ORA_piet_retief
		}

		# Gerrit Maritz gives ORA an early commando figure distinct from Potgieter.
		# No vanilla template exists for him, so this is the one remaining custom
		# Oranje start character in the roster.
		create_character = {
			first_name = Gerrit
			last_name = Maritz
			historical = yes
			culture = cu:boer
			religion = rel:protestant
			ig_leader = yes
			birth_date = 1797.3.1
			interest_group = ig_armed_forces
			ideology = ideology_jingoist_leader
			traits = {
				direct
				persistent
				popular_commander
			}
		}
	}
}
