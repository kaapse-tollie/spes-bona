# Startup entrypoints only; full definitions live in character_templates.

CHARACTERS = {
	c:ORA ?= {
		create_character = {
			template = ORA_hendrik_potgieter
			ruler = yes
			ig_leader = no
			is_general = yes
			interest_group = ig_landowners
			on_created = {
				set_ideology = ideology:ideology_sb_volksraad_elitism_leader
				remove_trait = cruel
				remove_trait = ambitious
				remove_trait = popular_commander
				add_trait = imperious
				add_trait = pious
				add_trait = expert_offensive_planner
				add_trait = plains_commander
				add_trait = surveyor
			}
		}

		create_character = {
			template = ORA_piet_retief
		}

		create_character = {
			template = ORA_gerrit_maritz
		}
	}
}
