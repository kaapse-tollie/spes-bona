# Startup entrypoints only; full definitions live in character_templates.

CHARACTERS = {
	c:ORL ?= {
		create_character = {
			template = ORL_jonker_afrikaner
		}

		create_character = {
			template = ORL_jan_jonker_afrikaner
		}
	}
}
