# Startup entrypoints only; full definitions live in character_templates.

CHARACTERS = {
	c:XHR ?= {
		create_character = {
			template = XHR_sandile
		}

		create_character = {
			template = XHR_maqoma
		}
	}
}
