DIPLOMACY = {
	# With the V2 treaty setup now authored in history, keep the early
	# Boer-British cooling-off period here as history too. This avoids the
	# after-lobby treaty normalizer that produced visible startup banners and
	# broken-obligation scars.
	c:ORA ?= {
		create_bidirectional_truce = {
			country = c:GBR
			months = 240
		}
	}
}
